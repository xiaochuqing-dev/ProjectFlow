name: ProjectFlow V3.7.4 Quality Gates

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      run_real_model:
        description: "使用 Secret 执行一次受预算限制的 GLM 真实验收"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  backend-unit-and-h2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - name: 后端单元测试与 H2 兼容回归
        working-directory: backend
        run: mvn -B test
      - name: 保存 JUnit 报告
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-junit
          path: backend/target/surefire-reports

  postgres-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - name: PostgreSQL Testcontainers 真实集成测试
        working-directory: backend
        run: mvn -B -Ppostgres-it verify
      - name: 保存 PostgreSQL 测试报告
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: postgres-junit
          path: backend/target/failsafe-reports

  frontend-quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - working-directory: frontend
        run: npm ci
      - name: TypeScript 与生产构建
        working-directory: frontend
        run: npm run lint && npm run build
      - name: 前端契约测试
        working-directory: frontend
        run: npm run test:contracts

  hermes-mcp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: ProjectFlow 只读 stdio MCP 契约与故障恢复
        run: python integrations/hermes/test_projectflow_mcp.py

  obsidian-projection:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Obsidian 投影完整性、增量、冲突与恢复
        run: python integrations/obsidian/test_projectflow_obsidian.py

  browser-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - working-directory: frontend
        run: npm ci
      - working-directory: frontend
        run: npx playwright install --with-deps chromium
      - name: Playwright 核心流程
        working-directory: frontend
        run: npm run test:e2e
      - name: 保存浏览器失败证据
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            frontend/playwright-report
            frontend/test-results
          if-no-files-found: ignore

  sensitive-content:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 阻止明显密钥进入仓库
        shell: bash
        run: |
          if git grep -nEI '(sk-[A-Za-z0-9_-]{20,}|ark-[A-Za-z0-9-]{20,}|Bearer [A-Za-z0-9._-]{24,})' -- ':!*.md' ':!*.txt'; then
            echo "检测到疑似密钥或 Authorization 值"
            exit 1
          fi

  optional-real-provider:
    if: github.event_name == 'workflow_dispatch' && inputs.run_real_model
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
      - name: 真实 GLM Provider、18-case 与项目理解端到端验收
        working-directory: backend
        env:
          PROJECTFLOW_RUN_REAL_MODEL: "true"
          PROJECTFLOW_REAL_MODEL_API_KEY: ${{ secrets.PROJECTFLOW_REAL_MODEL_API_KEY }}
          PROJECTFLOW_REAL_MODEL_PROVIDER: "GLM"
          PROJECTFLOW_REAL_MODEL_BASE_URL: "https://ark.cn-beijing.volces.com/api/coding/v3"
          PROJECTFLOW_REAL_MODEL_NAME: "glm-5.2"
          PROJECTFLOW_REAL_MODEL_TYPE: "OPENAI"
          PROJECTFLOW_REAL_MODEL_PROTOCOL: "OPENAI_RESPONSES"
          PROJECTFLOW_REAL_MODEL_TIMEOUT_SECONDS: "600"
          # Reasoning and visible JSON share this Provider ceiling. The Scout
          # starts at its 16k task ceiling; only a detected truncation can use
          # the explicitly configured GLM 5.2 recovery headroom.
          PROJECTFLOW_REAL_MODEL_MAX_TOKENS: "65536"
        run: |
          if [ -z "$PROJECTFLOW_REAL_MODEL_API_KEY" ]; then
            echo "SKIPPED: 未提供安全测试 Key"
            exit 0
          fi
          mvn -B -Dtest=ProjectFlowRealProviderProbeIT test
          mvn -B -Dtest=ProjectFlowRealModelEvalIT test
          mvn -B -Dtest=ProjectUnderstandingRealModelIT test
      - name: 保存内部真实模型评测工件
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: projectflow-real-model-eval
          path: backend/target/projectflow-eval/real
          if-no-files-found: ignore
