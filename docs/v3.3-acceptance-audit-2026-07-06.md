# ProjectFlow V3.3 Acceptance Audit

Date: 2026-07-06

| # | Result | Evidence |
| --- | --- | --- |
| 1 | Pass | Dashboard retains project creation and `ZipImportPanel`; `v33-pending-workflow.test.mjs`. |
| 2 | Pass | `ProjectAccessCard` retains local project path binding. |
| 3 | Pass | Primary UI uses 待整理变更 / 分析新变化; frontend language tests. |
| 4 | Pass | `PendingChangeScanService` scans from `ProjectReviewCursor` to HEAD. |
| 5 | Pass | First scan is limited to 30 commits; rewritten history uses time-range fallback with a warning. |
| 6 | Pass | `DevelopmentSegmentationService` creates persisted development segments. |
| 7 | Pass | Small input is bounded to 1–3 segments; service test. |
| 8 | Pass | Medium input is bounded to 2–5 segments; service test. |
| 9 | Pass | Large input is chunked by 25 and reduced to 3–8 segments; service test. |
| 10 | Pass | Model JSON requires `evidenceRefs`, `confidence`, and `needsUserReview=true`; model-enricher tests. |
| 11 | Pass | `SegmentEvidenceValidator` intersects model claims with real atoms/files and rejects evidence-free output. |
| 12 | Pass | Confirmation supports NEW_SEDIMENT, MERGE_EXISTING, EVIDENCE_ONLY, and IGNORE; controller tests. |
| 13 | Pass | Project sediment overview lists confirmed sediments instead of mapping all archive fields. |
| 14 | Pass | Unsupported subjective fields are behind the compatibility archive entry and absent from the default overview. |
| 15 | Pass | Sediment detail exposes separately labeled developer notes; only notes are patchable. |
| 16 | Pass | `/tasks` is 沉淀确认 and old AiSuggestion records stay in a collapsed compatibility section. |
| 17 | Pass | `/project-intelligence` is 项目沉淀 with focused sediment detail routes. |
| 18 | Pass | Versioned `AGENT_PROTOCOL.md`, `agent-results`, template, and bounded AGENTS entry are present; legacy pointer retained. |
| 19 | Pass | Read-only bridge health reports missing protocol/rules; existing 写入/刷新协议 action provides recovery. |
| 20 | Pass | Root `start.bat` delegates to the embedded launcher; embedded check reports V3.3, tools, and data path. |
| 21 | Pass | README documents V3.3 positioning, workflow, source roles, and compatibility. |
| 22 | Pass | Full backend suite, all frontend tests, and production build pass; existing auth/project/model/zip/output paths remain covered. |
| 23 | Pass | GitHub CLI status/refresh is optional enrichment and fails open with warnings. |
| 24 | Pass | Missing gh, login, remote, timeout, or malformed JSON does not block local Git; service tests. |
| 25 | Pass | No `--show-token`; no token persistence or response field; fixed commands only. |
| 26 | Pass | README explicitly states local Git primary, Agent result enrichment, and GitHub CLI optional. |
| 27 | Pass | AGENTS contains a marked V3.3 entry without replacing existing local rules. |
| 28 | Pass | Versioned `.projectflow/context` profile, requirements, decisions, risks, and update history reflect V3.3. |

## Verification evidence

- Backend: `mvn.cmd -q test` — 19 suites, 87 tests, 0 failures/errors/skips.
- Frontend: `node --test tests/*.test.mjs` — 17 tests passed.
- Frontend: `npm.cmd run build` — production build and TypeScript check passed.
- Launcher: `start-projectflow-embedded.ps1 -CheckOnly` — Maven/npm/data checks passed.
- Security: fixed ProcessBuilder arguments, bounded command timeouts/output, ownership checks, repository-relative Agent-result paths, no raw HTML or GitHub token flag.

The long-running interactive launcher was not left running during automated verification; its dependency/startup preflight and both application build paths were verified.
