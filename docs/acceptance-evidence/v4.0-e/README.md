# V4.0-E additive acceptance evidence

Status: PASS. DESKTOP_SHELL_TECHNICAL_ENTRY = READY_FOR_POC. Independent MODEL_REVIEW is PASS for both projects; Owner Review is NOT_REVIEWED. Actual required push/PR Quality, Windows and OSV passed on the head recorded in ci.json; facts-only delivery backfill is separately verified in Draft PR #25.

Base: PR #24 `81730744a325ccc2391009b367ca4ba95aef6d14`. Prior V4.0-D failures and both NEEDS_REVISION reviews remain in their original directory.

Evidence records measured request attempts, safe normalized diagnostics, test results and actual visible product output. Credentials, credential-bearing endpoints, source documents, patches, model prompts, raw responses and reasoning are excluded. Transient execution data remains outside the deliverable in ignored task-local storage.

Current observed milestone: both frozen real projects completed Current Understanding using the production Gateway, official OpenAI Responses SDK streaming, `gpt-5.6-sol` / `xhigh`. Each used two requests with actual upstream usage. Both unchanged reruns used zero requests. The upstream origin of the V4.0-D HTTP/2 reset remains UNDETERMINED.

Final History, zero-call reruns, source audits, independent visible review, local backend/PostgreSQL/browser and Windows proofs are recorded. final-visible/ is additive; all earlier review inputs and failures remain unchanged. GitHub required CI passed; Owner review and any merge decision remain separate.
