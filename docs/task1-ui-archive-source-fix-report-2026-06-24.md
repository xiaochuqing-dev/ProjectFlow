# 任务一交付报告：项目画像与每日回顾追溯修正

日期：2026-06-24

## 完成内容

1. 项目画像「已完成能力」
   - 主页面不再直接展开 `completedCapabilities` 原始文本，避免大段文件路径铺满页面。
   - 新增 `/project-intelligence/capabilities` 能力清单页，只展示能力点，不展示 zip/Git 原始路径。
   - 新增能力解析前端防线：历史数据如果已经含路径，会转成可读能力点或过滤路径噪音。
   - 后端 zip 导入建议不再把 `moduleStructure` 写入「已完成能力」，改为生成结构导入、技术栈、测试、启动、部署等能力摘要。
   - 采纳结构化变更写入项目档案时，会清理「代表文件/影响文件」和裸路径行。

2. 项目档案入口卡片
   - 右侧入口卡增加不同色条和深色数量标记，避免只靠时间区分。
   - 新增「能力清单」入口，和成长时间线、字段来源链、档案变化、分析记录分开。

3. 每日回顾来源
   - 原来的 4 个重复跳转按钮合并为 1 个「查看全部来源」入口。
   - 4 类来源保留为状态标记，不再伪装成独立按钮。
   - 新增 `/dev-logs/sources/[sourceId]` 来源详情页；进入每日来源列表后，每张来源卡可以继续点开追溯详情。

## 验收结果

- `frontend`: `npm.cmd run build` 通过。
- `frontend`: 全部 `tests/*.mjs` 静态验收通过。
- `backend`: `mvn.cmd -q test` 通过。
- 新增断言覆盖：
  - zip 导入建议不再把 `src/app/page.tsx`、`package.json` 等路径写作能力。
  - 项目画像存在独立能力页和紧凑能力入口。
  - 每日回顾来源只有一个汇总入口。
  - 每日来源卡存在详情跳转。

## 体积处理

本轮没有新增依赖。`npm.cmd run build` 会重新生成 `.next` 构建产物；它属于可再生输出，不是源码膨胀。第二份审查报告会单独说明前端占比和可清理边界。
